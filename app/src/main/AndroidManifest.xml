<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jxn.androidwidgetlearn">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".recylerview.RecyclerViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".onActivityResult.ResultActivity"/>
        <activity android:name=".onActivityResult.ResultActivityB"/>
        <activity android:name=".layoutWeight.LayoutWeightActivity"/>
        <activity android:name=".drawer.DrawerLayoutActivity"/>
    </application>

</manifest>